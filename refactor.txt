Refactor this SolidJS file into smaller, reusable components following best practices. No planning - start immediately.

REQUIREMENTS:1

Never egenrate any readme or summary file, no explanation eneded

1. Extract business logic into custom hooks (src/hooks/)

2. Create small, focused UI components (src/components/)

3. Separate concerns: data fetching, filtering, UI rendering

4. Make components reusable with clear props interfaces

5. Keep each component under 100 lines

6. Use TypeScript with proper types

7. Follow SolidJS patterns: createMemo, createSignal, createResource

8. Extract repeated UI patterns into shared components

9. Move complex computations to custom hooks

10. Ensure all code works - check with getDiagnostics

STRUCTURE:

- Hooks: useXxx.ts files for logic (data fetching, filtering, state management)

- Components: Small, focused UI components with clear single responsibility

- Main file: Should be minimal, just composing hooks + components

SOLIDJS BEST PRACTICES:

- Prefer createMemo for derived state

- Extract resource logic to custom hooks

- Keep components pure and presentational when possible

- Use props destructuring carefully (don't destructure signals)

- Co-locate related components in folders

- Use Show/For from solid-js, not if/map

Start refactoring now. Create all necessary files and update the main file.